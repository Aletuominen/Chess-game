#include "main.h"
#include "functions.h"
#include "initialise.h"

// boardOrder is constant, try to figure how to use it without passing it around. Only printBoard needs it.


int main() {

	setupBoard();

	/* DEBUG PRINT
	for (auto it : boardPosition) {
		cout << it.first << " " << it.second << endl;
	}
	*/


}